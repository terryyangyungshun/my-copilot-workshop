# CHANGELOG

## [0.2.0] - Step 2
新增
- 深色模式切換按鈕（標題列右側），可在淺色 / 深色之間切換
- 深色模式會記住使用者偏好，使用 localStorage 儲存
- 若使用者未手動切換主題，預設會跟隨作業系統的 `prefers-color-scheme`
- 篩選按鈕：全部、未完成、已完成（輸入框下方），可依篩選顯示清單

變更
- 使用 CSS 變數管理淺/深色主題色彩，不在各處寫死色碼
- 清單篩選會顯示對應的空提示文字；底部的「未完成」計數始終顯示整體數量

## [0.1.0] - Step 1
新增
- 新增待辦：輸入框 + 新增按鈕，支援按 Enter 新增，會忽略空白內容
- 完成勾選：每項左側勾選框，勾選後文字加上刪除線並淡化
- 刪除：每項右側刪除按鈕，可移除該筆待辦
- 未完成計數：底部顯示「未完成:N 項」
- localStorage 永續化：待辦資料會儲存在 localStorage，重新整理後仍保留
